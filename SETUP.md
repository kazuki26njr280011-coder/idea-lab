# セットアップ手順（Deno Deploy）

サーバーを借りたり、トンネルを通したりする必要がない。
GitHubに置いて繋ぐだけで、httpsのURLがもらえる。

**クレジットカード不要。スリープなし。**

かかる時間：だいたい20分。

---

## 全体の流れ

```
1. Gemini APIキーを取る
2. GitHubにコードを置く
3. Deno Deploy に繋ぐ
4. 環境変数を入れる
5. 自分で1周して確認する
```

---

## 1. Gemini APIキーを取る（3分）

1. https://aistudio.google.com/apikey を開く
2. Googleアカウントでログイン（親の許可の範囲で使うこと）
3. 「APIキーを作成」を押す
4. `AIza...` で始まる文字列をコピーしておく

**このキーは絶対に人に見せない。** スクショに写り込むのも危ない。
GitHubにも置かない（あとで別の場所に入れる）。

---

## 2. GitHubにコードを置く（7分）

GitHubアカウントを持っていなければ https://github.com で作る。

### ブラウザだけでやる方法

1. https://github.com/new を開く
2. Repository name に `idea-lab` と入れる
3. **Public** のままでよい（APIキーは入っていないので問題ない）
4. 「Create repository」を押す
5. 次の画面の「uploading an existing file」というリンクを押す
6. `idea-lab-deno` フォルダの**中身**をドラッグして上げる

上げるもの：

```
main.js
config.js
deno.json
public/          ← フォルダごと
```

`SETUP.md` や `README.md` は上げても上げなくてもいい。

7. 下の「Commit changes」を押す

**注意：** `idea-lab-deno` フォルダごと上げると階層がずれる。
リポジトリの直下に `main.js` が見えている状態にすること。

---

## 3. Deno Deploy に繋ぐ（3分）

1. https://deno.com/deploy を開く
2. 「Sign in with GitHub」でログイン
3. 「New Project」を押す
4. さっき作った `idea-lab` リポジトリを選ぶ
5. Entrypoint（実行するファイル）に `main.js` を指定する
6. Deploy を押す

数秒で `https://idea-lab-xxxx.deno.dev` のようなURLが出る。
**これが配るURL。** 再起動しても変わらない。

---

## 4. 環境変数を入れる（2分）

プロジェクトの **Settings → Environment Variables** を開く。

| 名前 | 値 |
|---|---|
| `GEMINI_API_KEY` | 手順1でコピーしたキー |
| `ADMIN_KEY` | 自分で決める合言葉（例：`mugitani2026kansei`） |

入れたら保存し、もう一度デプロイし直す（Settingsの保存だけで反映されることもある）。

`ADMIN_KEY` を知っている人だけが集計を見られる。`1234` のような簡単なものにしないこと。

---

## 5. 自分で1周して確認する（10分）

**ここは必ずやること。**

1. `https://〜.deno.dev/` を開いて、最後まで通してみる
2. **条件Bの画面でAIの案が10個出るか**（一番失敗しやすいところ）
3. **条件Cで案を送ったあと、AIの指摘が3つ出るか**
4. `https://〜.deno.dev/admin.html?key=合言葉` を開いて、記録されているか見る

AIが出ない場合は、Deno Deploy の **Logs** タブにエラーが出ている。
だいたいAPIキーの入れ間違い。

### テストデータを消す

確認が終わったら、自分のテストデータを消す。混ざると結果が汚れる。

**Deno KVのデータを消す方法**

プロジェクトの **KV** タブを開き、`idea` と `vote` で始まるキーを削除する。
面倒なら、Deno Deploy でプロジェクトを作り直しても同じことになる。

（本番開始前にやること。始まってからやるとデータが飛ぶ）

---

## URL一覧

| URL | 誰に配るか | いつ |
|---|---|---|
| `/` | アイデアを出してくれる人 | 8月上旬 |
| `/evaluate.html` | 評価してくれる人 | 8月中旬〜 |
| `/admin.html?key=合言葉` | 自分だけ | いつでも |

**収集が先。** アイデアが6個未満だと評価画面は「準備中」と出る。

---

## 6. 事前チェックを依頼する

Slackの事前チェックスレッドに投げる。**アプリでも必要。**

```
@staff_rikuto_asayama
こんにちは。
「生成AIの使い方の違いによって、出てくるアイデアの質が変わるのか」
を明らかにすることを目的に、以下の調査を実施したいです。
事前チェックをお願いします。

Googleフォームではなく、自作のWebアプリで実施します。
・氏名、メールアドレスなどの個人情報は一切収集していません
・回答は任意であること、途中でやめてよいことを画面に明記しています
・入力内容はAIの返答を作るためGoogleのAPIに送信されます。その旨も明記しています
・データは統計的に処理し、リベ応用の発表以外には使用しません

URL：https://〜〜〜.deno.dev/
```

---

## 無料枠について

Deno Deploy の無料プランは月100万リクエスト、KVストレージ1GiB。
参加者30人程度なら使い切ることはまずない。

---

## お題を変えたいとき

`config.js` の上のほうを書き換えて、GitHubで保存すれば自動で反映される。
Deno Deploy はGitHubの変更を検知して勝手に入れ替えてくれる。

**ただし本番開始後は変えないこと。** 途中で条件が変わると比べられなくなる。

---

## Pterodactyl版との違い

| | Deno Deploy | Pterodactyl |
|---|---|---|
| https | 最初から付く | トンネルが必要 |
| URL固定 | 最初から固定 | 設定しないと変わる |
| 起動しっぱなし | 気にしなくていい | 止めると回答が取れない |
| データ保存 | Deno KV（自動） | `data/db.json`（要バックアップ） |
| 手間 | GitHubに置くだけ | ファイル配置・環境変数・トンネル |

こだわりがなければ Deno Deploy のほうが楽。
