// ===== 実験の設定 =====
// ここだけ書き換えれば内容を変えられる

export const THEMES = [
  { id: "t1", text: "学校の図書室の利用者を増やすには？" },
  { id: "t2", text: "文化祭の来場者を増やすには？" },
  { id: "t3", text: "近所のコンビニの売上を上げるには？" },
];

// 条件Bで見せるAIの案の個数
export const B_IDEA_COUNT = 10;

// アイデア出しの制限時間（秒）
export const TIME_LIMIT = 180;

// 評価フェーズのペア数（前半=やってみたい / 後半=思いつかなそう）
export const PAIRS_TRY = 10;
export const PAIRS_SURPRISE = 10;

// ===== プロンプト（サーバー側で固定。絶対にクライアントから渡さない） =====

export const PROMPT_B = (theme, n) =>
  `あなたは中学生の探究学習を手伝うアシスタントです。
次のテーマについて、アイデアを${n}個挙げてください。

テーマ：${theme}

条件：
- 1行に1個、番号をつけて簡潔に書く
- 1個あたり40字以内
- 説明や前置き、まとめは書かない
- アイデアの一覧だけを出力する`;

export const PROMPT_C = (theme, idea) =>
  `あなたは中学生の探究学習を手伝うアシスタントです。
次のアイデアに対して、弱点や見落としを指摘してください。

テーマ：${theme}
アイデア：${idea}

条件：
- 指摘は3個まで。1行に1個、簡潔に書く
- 1個あたり50字以内
- 相手を否定する言い方はせず、「〜は考えましたか？」のように問いかける形にする
- 改善案は出さない。あくまで指摘だけにとどめる
- 前置きやまとめは書かない`;
